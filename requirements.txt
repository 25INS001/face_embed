# CPU only. No GPU is requested by either deployment -- no nvidia.com/gpu
# limit, no runtimeClassName -- and detection/face_functions.py already picks
# its device with `torch.cuda.is_available()`, which returned False in the
# running container. The image was nonetheless carrying 4.3GB of nvidia/*,
# 592MB of triton and a CUDA build of torch that could never execute.
#
# torch and torchvision come from PyTorch's CPU index (see Dockerfile), so
# the pins below resolve to +cpu builds. Re-adding CUDA means restoring these
# 16 packages AND the index change AND giving the pods a GPU; it is not a
# one-line revert.
#
# opencv-python-headless because nothing here opens a window, and it drops
# the libgl1/libglib2.0-0 apt layer with it.

antlr4-python3-runtime==4.9.3
asgiref==3.11.0
certifi==2025.11.12
charset-normalizer==3.4.4
Django==5.2.9
djangorestframework==3.16.1
filelock==3.20.1
fsspec==2025.12.0
fvcore==0.1.5.post20221221
gunicorn==23.0.0
hf-xet==1.2.0
huggingface-hub==0.36.0
idna==3.11
iopath==0.1.10
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.4.2
numpy==2.2.6
omegaconf==2.3.0
opencv-python-headless==4.12.0.88
packaging==25.0
pillow==12.0.0
portalocker==3.2.0
PyYAML==6.0.3
regex==2025.11.3
requests==2.32.5
safetensors==0.7.0
sqlparse==0.5.4
sympy==1.14.0
tabulate==0.9.0
termcolor==3.2.0
tokenizers==0.22.1
torch==2.9.1
torchvision==0.24.1
tqdm==4.67.1
transformers==4.57.3
typing_extensions==4.15.0
urllib3==2.6.2
yacs==0.1.8
